# docker centos8

# FROM centos
# RUN yum install vim -y
# RUN yum install python39 -y
# WORKDIR /rushiquant
# COPY . .
# RUN rm -rf .git
# RUN rm -rf venv
# RUN pip3 install -r docs/requirements.txt
# RUN cp -rf /usr/bin/python3 /usr/bin/python
# RUN cp -rf /usr/bin/pip3 /usr/bin/pip

# docker 没有centos8的镜像了
# docker centos7

# FROM centos:centos7
# RUN yum install vim -y
# RUN yum install python3 -y
# WORKDIR /rushiquant
# COPY . .
# RUN rm -rf .git
# RUN rm -rf venv
# RUN pip3 install -r docs/requirements.txt
# RUN cp -rf /usr/bin/python3 /usr/bin/python
# RUN cp -rf /usr/bin/pip3 /usr/bin/pip

# docker centos7 没有websockets 10.1版本,这个是基于ubuntu的
# docker python:3.9.10 但是这个包比较大

FROM python:3.9.10
RUN apt update -y
RUN apt install vim -y
WORKDIR /rushiquant
COPY . .
RUN rm -rf .git
RUN rm -rf venv
RUN python -m pip install --upgrade pip
RUN pip3 install -r docs/requirements.txt
